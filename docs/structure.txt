TOOL-2025/
├─ AGENTS.md               # Projektanweisungen
├─ README.md               # Kurzer Projektüberblick (noch auszubauen)
├─ docs/
│  ├─ analysis-report.md   # Detaillierter Analysebericht (Professional Edition)
│  ├─ developer-guide.md   # Entwicklerhandbuch (Professional Edition)
│  └─ structure.txt        # Diese Strukturübersicht
├─ .eslintrc.json          # ESLint-Konfiguration für HTML/JS-Linting
├─ fortschritt-info.txt    # Aktuelle Release-Fortschrittsmeldung
├─ info-you-need.txt       # Kurzreferenz mit Setup-/Befehlsübersicht
├─ index.html              # Monolithische App (UI, Styles, Logik, Plugin-Manager, Backup-Prüfung, Layout-/Konfigurations-Assistent, Smart-Error-Guard + `guardAction` mit Präventionshinweisen, Start-Check mit Speicher-Fallback, Feedback-Badge, Guard-gestützte Exporte & Debug-Download)
├─ tools/                  # Hilfsskripte (Start-Routine, spätere Build-Tools)
│  └─ start-tool.js        # Startskript: legt Ordner an, startet Server, öffnet Browser automatisch
├─ node_modules/           # Installierte Abhängigkeiten (npm)
├─ package.json            # Scripts, Abhängigkeiten, Projekt-Metadaten
├─ package-lock.json       # Lockfile für reproduzierbare Installationen
├─ schemas/
│  └─ backup-schema.json   # JSON-Schema für Backups
├─ tests/
│  └─ app.test.js          # Node-basierte Tests (Playlist, Plugins, Backup, Guard-Hinweise, Start-Check-Report)
└─ todo.txt                # Backlog mit priorisierten Verbesserungen
